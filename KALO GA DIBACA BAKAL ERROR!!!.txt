********************************HOLICBOT**************************************

Hello, let me introduce myself, I'm ICEKING (Gamer).
THIS IS A HELP FILE, FOR YOU TO SOLVE CERTAIN ERRORS THAT MAY OCCUR,
IF THE ERRORS CONTINUE AFTER DEBUGGING YOU CAN CONTACT ME!

========================================================

The Modules Are Separated?
Because there's a bug if you install it directly.

READ CAREFULLY!
$ pkg update && pkg upgrade
$ pkg install git
$ pkg install bash
$ pkg install nodejs
$ pkg install ffmpeg
$ pkg install imagemagick
$ pkg install libvips
$ pkg install yarn
$ pkg install mc
$ termux-setup-storage
$ cd storage
$ cd downloads
$ cd sych
$ cp -r sych $HOME

==========================================================

COMMAND 2!!

After you run yarn and it's finished, you immediately extract
node_module.zip wherever you want, as long as it's not outside the download folder
then later when there's a red warning during (figlet), just All in
if it's finished, replace it with (sharp), if sharp is finished
don't run it directly!
you run this command:

$ npm install --cpu=wasm32 sharp

if that command is finished, then:

$ npm start

later you'll be asked to enter a number 62xxxxx and a pairing code xxXXxXX
